package com.example.sample

// Test fixture — the integration tests open this file in the editor and
// expect the panel to scope to it. The contents don't have to compile;
// the Kotlin extension just needs to recognise the language id, and the
// extension's preview detection looks at the generated previews.json
// (under build/compose-previews/) rather than parsing this file.
fun StaticPreview() {}
fun AnimatedPreview() {}
